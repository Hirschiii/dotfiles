#!/usr/bin/env sh

if [ -n "$TMUX" ] ; then
    # tmux session running
    # tmux split-window -h "nvim \"$*\""
    nvim "$*"
else
    # Remove option --tab for new window
    # st -e nvim $* &
    nvim "$*"
fi
