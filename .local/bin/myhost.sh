#!/bin/bash

# echo nmcli -p device show | grep 'IP4.ADDRESS' | head -1 | awk '{ print $2 }') "|" $(xprop -root -spy | awk '/^_NET_CURRENT_DESKTOP/ { print ($3 + 1) ; fflush(); }') | $(hostname)"

echo ${echo "test"} "haha"
